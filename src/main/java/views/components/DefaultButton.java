package views.components;

import javax.swing.*;
import java.awt.*;

public class DefaultButton extends JButton {
    public DefaultButton(String title) {
        super(title);
        setBackground(new Color(160, 160, 64));
    }
}
